{
  "name": "monthsary-app",
  "version": "1.0.0",
  "private": true,
  "description": "9th Monthsary app with a real backend (Postgres) so music, photos, and letters persist.",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@supabase/server": "^1.1.0",
    "dotenv": "^16.4.5",
    "express": "^4.22.2",
    "multer": "^2.0.0",
    "pg": "^8.12.0",
    "postgres": "^3.4.9"
  }
}
